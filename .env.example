# Supabase — project "visionboard" (org: jpatten1218's Org, region: us-east-1)
# Values come from the Supabase dashboard: Project Settings → API.

# Safe to expose to the browser.
NEXT_PUBLIC_SUPABASE_URL="https://hrhsgmqrsmyqayzlnvbt.supabase.co"

# SECRET. Bypasses row-level security — server-side only, never in a client
# component and never prefixed with NEXT_PUBLIC_. Rotate it if it ever leaks.
SUPABASE_SERVICE_ROLE_KEY=""

# Optional gate while the board has no login. When set, the app asks for this
# passcode once and stores a signed cookie. Leave empty to run wide open.
VISION_BOARD_PASSCODE=""
