/**
 * Section 05: three to five goals in focus, everything else waiting. Lives
 * outside the actions module because a "use server" file may only export
 * async functions.
 */
export const FOCUS_CAP = 5;

/** The lower end of the range, used only in copy. */
export const FOCUS_FLOOR = 3;
